export interface BasePageDto {
    pageIndex: number,
    pageSize: number
}

export interface BasePageVo {
    createBy: string,
    createTime: string,
    updateBy: string,
    updateTime: string,
}
